<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.homeindoctor.dih.dao.GoodsDao">

<!-- 상품 등록 -->
<insert id="goodsUploadForm" parameterType="com.homeindoctor.dih.dto.GoodsDto">
    insert into goods (goods_name, goods_img, goods_intro, goods_price, stock)
    values (#{goods_name}, #{goods_img}, #{goods_intro}, #{goods_price}, #{stock})
</insert>
</mapper>